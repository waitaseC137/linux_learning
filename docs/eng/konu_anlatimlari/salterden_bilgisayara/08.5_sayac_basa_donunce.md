# 🔁 From Switches to a Computer — Interlude: When the Counter Wraps (CWE-190)

> Last lesson `65535 + 1 = 0` happened and you passed over it as a curiosity. It isn't one.
> This interlude explains **which mathematics** stands behind that single line, and why it
> has been feeding one of the most productive vulnerability classes for years.

> The whole claim of this lesson in one sentence:
>
> **The machine does not run on `(ℕ, +)`, it runs on `(ℤ/2ⁿℤ, +)`. CWE-190 is assuming a
> property of the first while standing in the second.**

> *(Interlude: no circuits, no new level. But the experiments at the end are done in levels
> you've already solved — keep the game open.)*

---

## 📋 Table of Contents

- [Which World Does the Machine Live In](#which-world-does-the-machine-live-in)
- [Modular Arithmetic in Five Minutes](#modular-arithmetic-in-five-minutes)
- [The Exact Location of the Bug](#the-exact-location-of-the-bug)
- [Two Directions — The Second Is Sneakier](#two-directions--the-second-is-sneakier)
- [Harmless On Its Own](#harmless-on-its-own)
- [Deriving the Correct Check](#deriving-the-correct-check)
- [And the Compiler Can Delete Your Check](#and-the-compiler-can-delete-your-check)
- [What a CWE Is, How the Chain Works](#what-a-cwe-is-how-the-chain-works)
- [🎮 Try It in the Game](#-try-it-in-the-game)

---

## Which World Does the Machine Live In

The model in everyone's head is this: a variable holds a number, and numbers go on forever.

**Wrong.** The set an `n`-bit variable lives in is finite:

```
Uₙ  =  { 0, 1, 2, … , 2ⁿ − 1 }          number of elements: 2ⁿ
```

For 16 bits, `U₁₆ = {0 … 65535}`. And this set is **closed** — you cannot get out of it. If
an addition's result spills outside, the result doesn't stay outside; it **wraps back
around.**

This kind of structure has a name in mathematics: **`ℤ/2ⁿℤ`**, that is, "the integers,
divided into `2ⁿ` boxes".

And addition is no longer ordinary addition either:

```
the machine's addition  =  (a + b)  mod  2ⁿ
```

---

## Modular Arithmetic in Five Minutes

You already do it: **the clock.** It's 10 o'clock, add 5 hours → you don't say 15, you say
**3**. Because at 12 the clock fills up and goes back to the start.

### Two notations, don't mix them up

| written | what | what it gives you |
|---|---|---|
| `a mod m` | an **operation** — take the remainder | a **number** (`0 … m−1`) |
| `a ≡ b (mod m)` | a **relation** — they're in the same box | true / false |

```
17 mod 12 = 5              ← operation
17 ≡ 5 (mod 12)            ← relation
```

The `≡` sign does not mean "equals", it means **"in the same class"**. In 1801 Gauss
deliberately chose something that resembles `=` but isn't it.

### The definition

```
a ≡ b  (mod m)      ⟺      m  divides  (a − b)  exactly
                    ⟺      a and b leave the same remainder
```

Note: `b` **does not have to be** the remainder. All of these are true:

```
17 ≡ 5 ≡ 29 ≡ −7      (mod 12)
```

All of them in the same box: `{ … −19, −7, 5, 17, 29, 41 … }`

> 🔑 And lesson 09's subject drops straight out of this:
> **`−1 ≡ 2ⁿ − 1  (mod 2ⁿ)`**
> At 16 bits: `−1 ≡ 65535`. Two's complement is not a separate invention — it's just where
> the negative numbers sit on this circle.

> ⚠️ **Notation warning:** Some texts write machine addition as `⊕`. In security and
> cryptography writing, `⊕` almost always means **XOR**. Don't confuse the two — in this
> lesson we always write `(mod 2ⁿ)` explicitly.

---

## The Exact Location of the Bug

The proposition in the programmer's head, never questioned, is this:

```
∀ a, b ∈ Uₙ ,  b > 0    :    a + b  >  a          ← MONOTONICITY
```

True in `ℤ`. In `ℤ/2ⁿℤ` it's **partly** true:

```
(a + b) mod 2ⁿ  >  a       ⟺       a + b  <  2ⁿ
```

Which means the code is broken **not on all inputs**, only on this set:

```
H  =  { (a, b) ∈ Uₙ × Uₙ  :  a + b ≥ 2ⁿ }
```

> 🔑 This is where you see why CWE-190 is hard to find: **there is no visible mistake in the
> code.** `n + 1` is written correctly. The bug surfaces only the moment the inputs land in
> the set `H` — and tests usually never go there.

---

## Two Directions — The Second Is Sneakier

| direction | what happens | at 16 bits |
|---|---|---|
| **overflow upward** | a large number wraps to a small one | `65535 + 1 = 0` |
| **wrap downward** | a small number becomes **enormous** | `0 − 1 = 65535` |

Wrapping downward hurts more in practice, because unsigned types have nothing below zero:

```c
size_t len = 0;
memcpy(dst, src, len - 1);   //  len-1 = 18446744073709551615
```

Nobody considered that a length could be zero; `len - 1` isn't a decrement, it's **a full
lap backwards.**

---

## Harmless On Its Own

This matters: CWE-190 is a **weakness**, not a vulnerability by itself. A counter can wrap
and nothing happens.

For it to be dangerous, the overflowed value has to go to **one of these four places**:

```
1.  a memory allocation size  →  malloc(n)        →  a region SMALLER than needed
2.  a bounds check            →  if (i < n)       →  the check is TOPPLED
3.  a loop counter            →  for(; i<n; )     →  never ends / spins too long
4.  an array index            →  buf[i]           →  out-of-bounds access
```

> 🔑 This is the question to ask while auditing: **where does this overflowable number go?**
> If it goes to none of those four, it's noise, not a finding. That distinction is what
> separates a good security report from noise.

---

## Deriving the Correct Check

Why doesn't this check work?

```c
if (a + b > MAX)  return ERROR;
```

The answer is merciless: `a + b` enters the check **already wrapped.** And by definition the
result is always inside `Uₙ`, that is, **always** `≤ MAX`.

> ⚠️ This check doesn't merely miss sometimes. It **never** fires — it's an expression that
> is mathematically, identically false.

Don't memorise the right one, **derive it.** What we want is `a + b ≤ MAX`. To ask the
question without ever performing the wrap, move `b` to the other side:

```
a + b ≤ MAX
a     ≤ MAX − b
```

And `MAX − b` is **always safe**, because `b ≤ MAX` means the result cannot be negative —
the subtraction never wraps.

```c
if (a > MAX - b)  return ERROR;        // the check asked WITHOUT overflowing
```

### For multiplication

```
a · b < 2ⁿ    ⟺    b = 0   or   a ≤ ⌊MAX / b⌋
```

Multiplication is more dangerous, because the set `H` is much bigger: to overflow an
addition you need two **large** numbers; for a multiplication two **medium** ones suffice.

---

## And the Compiler Can Delete Your Check

In C two types behave differently, and the difference is vital:

| type | overflow behaviour |
|---|---|
| **unsigned** | defined — wraps `mod 2ⁿ`, guaranteed |
| **signed** | **undefined behaviour (UB)** |

"Undefined" means the compiler **has the right to assume it never happens**:

```c
int a, b;
if (a + b < a)        // the classic overflow check
    return ERROR;
```

The compiler's reasoning: *"Signed overflow is UB, so it doesn't happen. If it doesn't
happen, `a+b` is always greater than or equal to `a`. Therefore this condition is always
false."*

**And it deletes the check during optimisation.**

> ⚠️ You genuinely see this in reverse engineering: the check exists in the source, and
> there is no counterpart to it in the disassembly. It isn't a compiler bug — it's a licence
> the standard grants. That's why, before saying "there's no check" in a binary, you have to
> look not at the source but at the **generated code.**

### The right ways to do it

```c
if (__builtin_mul_overflow(count, size, &total)) return ERROR;   // true if it overflows
buf = calloc(count, size);        // NOT malloc(count * size) — calloc checks
// while testing:  clang -fsanitize=integer
```

---

## What a CWE Is, How the Chain Works

| | what |
|---|---|
| **CWE** | the **kind** of weakness — "integer overflow" |
| **CVE** | **one specific case** in a specific product |

CWE is MITRE's catalogue of vulnerability **types**. A CVE description usually says which
CWE it belongs to.

Our subject:

```
CWE-190   Integer Overflow or Wraparound       ← this lesson
CWE-680   Integer Overflow to Buffer Overflow  ← the chain's own name
CWE-787   Out-of-bounds Write                  ← the consequence
```

The existence of `CWE-680` is interesting: the observation "190 sometimes leads to 787" is so
common that it **got its own number.** In CWE this is called a *chain* — one weakness giving
birth to another.

The classic pattern is the very `+1` circuit you built in this lesson:

```c
n   = 65535;
buf = malloc(n + 1);        // n+1 WRAPS → malloc(0), zero bytes
for (i = 0; i <= n; i++)
    buf[i] = ...;           // writes 65536 times → out-of-bounds write
```

> 🔑 The subtlety: **it isn't that there's no bounds check — there IS one.** The programmer
> allocated `n + 1` bytes and thought correctly. But because *the check itself* overflowed,
> it saw a small number and waved it through. The overflow **toppled the check.**

The famous real-world example is **Stagefright** (Android, 2015): while parsing a media file
a size computation overflows, a smaller-than-needed region is allocated, and then the real
size is written into it.

---

## 🎮 Try It in the Game

There's no new level in this lesson — but **three levels you've already solved** are three
different faces of the same circle. Open the game and see these with your own hands:

**1. Increment — catch the moment it wraps**
```
input 65534  →  output 65535 ,  carry-out 0
input 65535  →  output     0 ,  carry-out 1     ← the lap completed
```
That carry wire you left dangling is announcing the moment the circle returns to the start.
In a real processor its name is the **carry flag**.

**2. Subtraction — see the downward wrap**
```
A = 0 , B = 1   →   output 65535
```
You went one step back from zero and landed at the other end of the circle. The same number
is both `65535` and `−1` — and **the wires don't tell you which.**

**3. Less than Zero — find the exact location of the boundary**
```
input 32767  →  output 0     (bit 15 = 0, positive)
input 32768  →  output 1     (bit 15 = 1, negative)
```
The sign bit is not a label bolted on afterwards; it is exactly **where the wrapping
boundary passes.**

> 💡 When you do all three in a row you'll see this: **there is only one circle.** Increment
> turns it forward, Subtraction turns it back, and Less than Zero tells you where on it you
> are. Three levels, three different questions, the same `ℤ/2¹⁶ℤ`.

---

## Summary — Keep in Mind

```
☐ The machine runs on (ℤ/2ⁿℤ, +), not (ℕ, +). Wrapping is the rule, not the exception.
☐ a mod m = an OPERATION (a number) · a ≡ b (mod m) = a RELATION (same box or not).
☐ ≡ is not "equals", it's "in the same class". b need not be the remainder.
☐ −1 ≡ 2ⁿ−1 (mod 2ⁿ). Two's complement isn't a separate invention, it's the circle's far end.
☐ The vulnerability isn't in the arithmetic, it's in the ASSUMPTION that "addition grows".
☐ The bug set H = {(a,b) : a+b ≥ 2ⁿ}. Outside it the code is flawless — hence hard to find.
☐ Two directions: overflow upward and the 0−1 wrap downward. The second is sneakier.
☐ Harmless alone. Dangerous if it feeds a size / bounds check / loop / index.
☐ Do the check BEFORE the overflow: a > MAX − b. A check afterwards is IDENTICALLY FALSE.
☐ unsigned overflow is defined; signed overflow is UB → the compiler MAY DELETE your check.
☐ CWE = the type, CVE = the case. The chain: CWE-190 → CWE-680 → CWE-787.
☐ A bounds check may exist; when the thing that overflows IS the check, it topples.
```

---

## 🔗 Related Topics

- [08.5.1_tasma_gercek_hayatta.md](./08.5.1_tasma_gercek_hayatta.md) — Real cases of the same mathematics: Stagefright, Ariane 5, Boeing 787
- [08_increment.md](./08_increment.md) — The circuit that produces the wrap, and the dangling carry wire
- [09_subtraction.md](./09_subtraction.md) — The circle's reverse direction: two's complement
- [04_teller_sayi_olunca.md](./04_teller_sayi_olunca.md) — `n` wires, `2ⁿ` patterns, `0…2ⁿ−1`
- [../binary_exploitation/00_buradan_basla.md](../binary_exploitation/00_buradan_basla.md) — Where the chain goes next: what you do with an out-of-bounds write

---

**Previous topic:** [08_increment.md](./08_increment.md)
**Next topic:** [08.5.1_tasma_gercek_hayatta.md](./08.5.1_tasma_gercek_hayatta.md)

*This lesson is part of the "From Switches to a Computer" series. The series moves along together with [nandgame.com](https://nandgame.com).*
